# Get a key at https://console.anthropic.com/settings/keys
# Set this as an environment variable in your deploy platform (e.g. Vercel
# project settings) — never commit a real key to git.
ANTHROPIC_API_KEY=sk-ant-...
